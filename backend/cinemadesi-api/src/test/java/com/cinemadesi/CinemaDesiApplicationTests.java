package com.cinemadesi;

import org.junit.jupiter.api.Test;

/**
 * Smoke test placeholder. We avoid {@code @SpringBootTest} here for the
 * skeleton phase so the build doesn't require a live Postgres / Flyway run.
 * A real context-loads test will be added once test config (Testcontainers)
 * is wired up in a later module.
 */
class CinemaDesiApplicationTests {

    @Test
    void placeholder() {
        // Intentionally empty — replaced by full integration tests in Module 13.
    }
}
