package com.cinemadesi.entity.enums;

/**
 * Role a user holds in a watch {@code Group}.
 * Matches the {@code group_members.role} CHECK constraint.
 */
public enum GroupRole {
    ADMIN,
    MEMBER
}
